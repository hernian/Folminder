<Window x:Class="Folminder.ConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Folminder"
        mc:Ignorable="d"
        Title="設定" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 設定内容エリア -->
        <GroupBox Grid.Row="0" Header="設定項目" Margin="10">
            <StackPanel Margin="10">
                <!-- ここに設定項目を追加 -->
                <TextBlock Text="設定項目をここに追加してください" 
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"
                          Foreground="Gray"/>
            </StackPanel>
        </GroupBox>

        <!-- ボタンエリア -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" 
                   HorizontalAlignment="Right" Margin="10">
            <Button Content="OK" Width="80" Height="30" 
                   Margin="0,0,10,0" IsDefault="True" 
                   Click="OkButton_Click"/>
            <Button Content="キャンセル" Width="80" Height="30" 
                   IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
